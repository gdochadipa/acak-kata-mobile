import 'package:flutter/material.dart';

class SuccessAnswerShowDialog extends StatelessWidget {
  // const SuccessAnswerShowDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
